# Translation runtime

Live translation uses Chrome's built-in Translator API in the standard app.

Local model folders are intentionally not bundled. The previous Xenova/OPUS-MT
runtime was too large and too slow for this tool.
